"""PDF summary export (stretch goal stub)."""

def generate_pdf(result: dict) -> bytes:
    """Return a placeholder text for PDF export."""
    text = "PDF Export is a stretch goal and not fully implemented yet."
    return text.encode("utf-8")
